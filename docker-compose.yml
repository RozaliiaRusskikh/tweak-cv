services:
  app:
    build:
      context: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - tweakcv_data:/app/data
      - tweakcv_output:/app/output
    restart: unless-stopped

volumes:
  tweakcv_data:
  tweakcv_output:
